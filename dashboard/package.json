{
  "name": "psychedelic-review-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "echo 'deploy via cloudflare/gh-pages — see README'"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@observablehq/framework": "^1.13.0"
  }
}
